DATABASE_URL=postgres://postgres:postgres@localhost:5432/research_feed
REDIS_URL=redis://localhost:6379
PUBMED_API_KEY=
PUBMED_TOOL=research-feed-app
PUBMED_EMAIL=you@example.com
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini
# Optional: point the AI worker at an OpenAI-compatible local endpoint
# such as LM Studio, vLLM, or Ollama's compatible API.
OPENAI_BASE_URL=
IMAGE_MODEL=gpt-image-1
ENABLE_AI_IMAGES=false
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000
# Optional: used in the OpenAlex API User-Agent (server-side citation lookups only)
OPENALEX_MAILTO=you@example.com
# Comma-separated origins allowed for credentialed browser requests (CORS). Must include your Next.js origin.
WEB_ORIGIN=http://localhost:3000
